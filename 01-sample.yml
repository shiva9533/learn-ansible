- name: INstall nginx
  hosts: all
  become: true
  tasks:
    - name: install Nginx
      ansible.builtin.yum:
        name: httpd
        state: latest
    - name: start nginx
      ansible.builtin.systemd:
        name: started
        enabled: yes

